export type ShopInfo = {
  id: number;
  title: string;
  img: string;
};

export type ProductsList = {
  id: number;
  title: string;
  img: string;
  price: string;
};

export type CartType = {
  id: number;
  title: string;
  img: string;
  price: string;
};
